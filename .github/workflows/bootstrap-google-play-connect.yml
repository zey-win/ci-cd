name: Bootstrap Google Play Connect

on:
  workflow_dispatch:
    inputs:
      google_cloud_project_id:
        description: Google Cloud project id used for Play publishing
        required: true
      google_client_id:
        description: Google OAuth Web Client ID
        required: true
      google_client_secret:
        description: Google OAuth Web Client Secret
        required: true
      render_service_id:
        description: Render service id for zeywin-connect
        required: true
        default: srv-d8948p3eo5us738g5dag

jobs:
  bootstrap:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Validate secrets
        shell: bash
        run: |
          set -euo pipefail
          test -n "${{ secrets.RENDER_API_KEY }}" || { echo "Missing RENDER_API_KEY secret."; exit 1; }
          test -n "${{ secrets.CONNECT_GITHUB_TOKEN }}" || { echo "Missing CONNECT_GITHUB_TOKEN secret."; exit 1; }

      - name: Bootstrap Render environment
        shell: bash
        env:
          RENDER_API_KEY: ${{ secrets.RENDER_API_KEY }}
          GITHUB_TOKEN: ${{ secrets.CONNECT_GITHUB_TOKEN }}
          GOOGLE_CLOUD_PROJECT_ID: ${{ inputs.google_cloud_project_id }}
          GOOGLE_CLIENT_ID: ${{ inputs.google_client_id }}
          GOOGLE_CLIENT_SECRET: ${{ inputs.google_client_secret }}
          RENDER_SERVICE_ID: ${{ inputs.render_service_id }}
        run: |
          set -euo pipefail
          node google-play-connect/scripts/bootstrap-admin.mjs

      - name: Redeploy connect portal
        shell: bash
        env:
          RENDER_API_KEY: ${{ secrets.RENDER_API_KEY }}
          RENDER_SERVICE_ID: ${{ inputs.render_service_id }}
        run: |
          set -euo pipefail
          curl -sS -X POST "https://api.render.com/v1/services/${RENDER_SERVICE_ID}/deploys" \
            -H "Authorization: Bearer ${RENDER_API_KEY}" \
            -H "Content-Type: application/json" \
            -d '{"clearCache":"do_not_clear"}' \
            | node -e 'let s="";process.stdin.on("data",d=>s+=d);process.stdin.on("end",()=>{const body=JSON.parse(s); console.log(`Render deploy: ${body.id || "created"}`);})'

      - name: Summary
        shell: bash
        run: |
          {
            echo "### Google Play Connect bootstrap"
            echo
            echo "- Portal: https://zey-win.github.io/connect/"
            echo "- Backend: https://zeywin-connect.onrender.com/operator"
            echo "- Status: https://zeywin-connect.onrender.com/status"
          } >> "$GITHUB_STEP_SUMMARY"
